class Solution {
    /**
     * @param {number[]} cost
     * @return {number}
     */
    minCostClimbingStairs(cost) {
        function dp(i) {

        }

        return Math.min(dp[0], dp[1]);
    }
}
